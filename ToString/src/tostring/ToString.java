package tostring;
public class ToString {
    public static void main(String[] args) {
     subOne subObject = new subOne(15,5,6); //This object must accept three integers because the constructor of its class says so
    }                                     //NOTE: the first value inputed is changed to a 12hr or regular time format
    
}
/*When this program is run, it will perform the statements inside the constructor*/